import React, { createContext } from 'react';

/**
 * Context data provider for content toggle block.
 *
 * @type {React.Context<{}>}
 */
const ContentToggleContext = createContext({});

/**
 * @module contentToggleContext
 */
export default ContentToggleContext;
